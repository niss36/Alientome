package com.events;

public enum GameEventType {

    GAME_START,
    GAME_EXIT,
    GAME_PAUSE,
    GAME_RESUME,
    GAME_RESET,
    GAME_DEATH,
    CONFIG_RESET,
    KEYBINDINGS_RESET,
    MESSAGE_SENT
}
